# ============================================
# Hexo博客性能优化配置
# 适用于安知鱼主题
# ============================================

## 1. 安装必要的优化插件

```bash
# 进入博客目录
cd azy-blog

# 图片优化
npm install hexo-lazyload-image
npm install hexo-filter-image-optimization

# 搜索功能
npm install hexo-generator-search

# RSS订阅
npm install hexo-generator-feed

# 站点地图
npm install hexo-generator-sitemap
npm install hexo-generator-baidu-sitemap

# PWA支持
npm install hexo-pwa

# SEO优化
npm install hexo-autonofollow
npm install hexo-filter-seo

# 压缩优化
npm install hexo-all-minifier

# 阅读统计
npm install hexo-leancloud-counter

# 相关文章
npm install hexo-related-popular-posts
```

## 2. Hexo主配置优化 (_config.yml)

```yaml
# ============================================
# 性能优化配置
# ============================================

# 渲染优化
render_drafts: false
post_asset_folder: true
relative_link: false
future: true

# 代码高亮优化
syntax_highlighter: highlight.js
highlight:
  line_number: true
  auto_detect: false
  tab_replace: '    '
  wrap: true
  hljs: false
  theme: atom-one-dark
  exclude_languages:
    - mermaid

prismjs:
  preprocess: true
  line_number: true
  tab_replace: ''

# 分页优化
index_generator:
  path: ''
  per_page: 10
  order_by: -date

# 分类和标签优化
category_map:
tag_map:

# 时间格式优化
date_format: YYYY-MM-DD
time_format: HH:mm:ss

# 分页设置
per_page: 10
pagination_dir: page

# 部署优化
deploy:
  type: git
  repo: git@github.com:mhd250/azy-blog.git
  branch: gh-pages
  message: "Site updated: {{ now('YYYY-MM-DD HH:mm:ss') }}"

# 忽略文件优化
ignore:
  - "**/node_modules/**"
  - "**/.git/**"
  - "**/source/_drafts/**"
```

## 3. 主题配置优化 (_config.anzhiyu.yml)

```yaml
# ============================================
# 安知鱼主题性能优化配置
# ============================================

# 预加载优化
preload:
  enable: true
  type: "scroll"  # 滚动时预加载
  rank: 5  # 预加载数量

# 图片懒加载
lazyload:
  enable: true
  field: site  # site/post
  placeholder: /img/loading.gif
  blur: true
  CDN: false

# 图片优化
photofigcaption:
  enable: true

# 代码优化
code:
  copy: true
  lang: true
  height_limit: 330
  word_wrap: false

# 评论系统优化
comments:
  use: valine  # 或其他
  lazyload: true  # 延迟加载

# 分析优化
analytics:
  enable: true
  lazyload: true  # 延迟加载统计代码

# 广告优化
google_adsense:
  enable: false  # 如不需要，关闭以提升速度

# 第三方CDN优化
cdn:
  enable: true
  provider: jsdelivr  # jsdelivr/unpkg/cdnjs
  custom_format: "https://cdn.jsdelivr.net/npm/${package}@${version}/${minified}"

# 字体优化
font:
  global:
    enable: true
    family: "Noto Sans SC"
    size: 16px
  code:
    enable: true
    family: "JetBrains Mono"
    size: 14px

# 动画优化
transition:
  enable: true
  type: "ease-in-out"
  duration: 0.3s

# 背景优化
background:
  enable: false  # 如不需要复杂背景，关闭以提升性能

# 特效优化
canvas:
  enable: false  # 关闭canvas特效以提升性能

# 鼠标特效
mouse:
  enable: false  # 关闭鼠标特效

# 雪花/樱花特效
season:
  enable: false  # 关闭季节特效
```

## 4. 图片优化配置

### 4.1 懒加载配置
在`_config.yml`中添加：

```yaml
lazyload:
  enable: true
  onlypost: false
  loadingImg: /img/loading.gif
  isSPA: false
  preloadRatio: 1.5
```

### 4.2 图片压缩配置
创建`scripts/image-optimization.js`：

```javascript
const imagemin = require('imagemin');
const imageminMozjpeg = require('imagemin-mozjpeg');
const imageminPngquant = require('imagemin-pngquant');
const imageminGifsicle = require('imagemin-gifsicle');
const imageminSvgo = require('imagemin-svgo');

hexo.extend.filter.register('after_generate', async function() {
  const sourceDir = hexo.source_dir;
  const publicDir = hexo.public_dir;

  // 压缩图片
  await imagemin([`${sourceDir}/**/*.{jpg,jpeg,png,gif,svg}`], {
    destination: publicDir,
    plugins: [
      imageminMozjpeg({ quality: 80 }),
      imageminPngquant({ quality: [0.6, 0.8] }),
      imageminGifsicle({ optimizationLevel: 2 }),
      imageminSvgo({
        plugins: [
          { removeViewBox: false },
          { cleanupIDs: false }
        ]
      })
    ]
  });

  console.log('✅ 图片压缩完成');
});
```

## 5. 缓存优化配置

### 5.1 浏览器缓存
在GitHub Pages根目录创建`_headers`文件：

```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 1; mode=block
  Referrer-Policy: strict-origin-when-cross-origin

/*.css
  Cache-Control: public, max-age=31536000, immutable

/*.js
  Cache-Control: public, max-age=31536000, immutable

/*.png
  Cache-Control: public, max-age=2592000

/*.jpg
  Cache-Control: public, max-age=2592000

/*.jpeg
  Cache-Control: public, max-age=2592000

/*.gif
  Cache-Control: public, max-age=2592000

/*.svg
  Cache-Control: public, max-age=2592000

/*.woff2
  Cache-Control: public, max-age=31536000, immutable
```

### 5.2 Service Worker (PWA)
创建`source/sw.js`：

```javascript
const CACHE_NAME = 'azy-blog-v1';
const urlsToCache = [
  '/',
  '/css/style.css',
  '/js/main.js',
  '/img/avatar.png'
];

self.addEventListener('install', event => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(cache => cache.addAll(urlsToCache))
  );
});

self.addEventListener('fetch', event => {
  event.respondWith(
    caches.match(event.request)
      .then(response => {
        if (response) {
          return response;
        }
        return fetch(event.request);
      })
  );
});

self.addEventListener('activate', event => {
  event.waitUntil(
    caches.keys().then(cacheNames => {
      return Promise.all(
        cacheNames
          .filter(name => name !== CACHE_NAME)
          .map(name => caches.delete(name))
      );
    })
  );
});
```

## 6. SEO优化配置

### 6.1 站点地图配置
在`_config.yml`中添加：

```yaml
sitemap:
  path: sitemap.xml
  template: ./sitemap_template.xml
  rel: false

baidusitemap:
  path: baidusitemap.xml
```

### 6.2 RSS订阅配置
```yaml
feed:
  enable: true
  type:
    - atom
    - rss2
  path:
    - atom.xml
    - rss2.xml
  limit: 20
  hub:
  content:
  content_limit: 140
  content_limit_delim: ' '
  order_by: -date
  icon: icon.png
  autodiscovery: true
  template:
```

### 6.3 Robots.txt
创建`source/robots.txt`：

```
User-agent: *
Allow: /
Allow: /archives/
Allow: /categories/
Allow: /tags/
Disallow: /js/
Disallow: /css/
Disallow: /fonts/

Sitemap: https://mhd250.github.io/azy-blog/sitemap.xml
```

## 7. 性能监控

### 7.1 添加性能监控脚本
在`_config.anzhiyu.yml`中添加：

```yaml
inject:
  head:
    - |
      <script>
        // 性能监控
        window.addEventListener('load', function() {
          setTimeout(function() {
            var timing = performance.timing;
            var loadTime = timing.loadEventEnd - timing.navigationStart;
            console.log('页面加载时间: ' + loadTime + 'ms');

            // 发送到分析平台（可选）
            if (window.gtag) {
              gtag('event', 'timing_complete', {
                'name': 'load',
                'value': loadTime
              });
            }
          }, 0);
        });
      </script>
```

## 8. 优化检查清单

### 部署前检查
- [ ] 所有图片已压缩
- [ ] CSS/JS已压缩
- [ ] 懒加载已启用
- [ ] 缓存策略已配置
- [ ] 站点地图已生成
- [ ] RSS订阅可用
- [ ] 搜索功能正常
- [ ] 移动端适配良好
- [ ] 加载速度 < 3秒
- [ ] Lighthouse评分 > 80

### 性能目标
- First Contentful Paint (FCP) < 1.8s
- Largest Contentful Paint (LCP) < 2.5s
- Time to Interactive (TTI) < 3.8s
- Total Blocking Time (TBT) < 200ms
- Cumulative Layout Shift (CLS) < 0.1

## 9. 常用优化命令

```bash
# 清理缓存
hexo clean

# 生成并部署
hexo g -d

# 本地预览
hexo s --debug

# 分析包大小
npm run analyze

# 压缩图片
npm run optimize-images

# 性能测试
npm run lighthouse
```

## 10. 推荐工具

### 在线测试工具
- [Google PageSpeed Insights](https://pagespeed.web.dev/)
- [GTmetrix](https://gtmetrix.com/)
- [WebPageTest](https://www.webpagetest.org/)
- [Lighthouse CI](https://github.com/GoogleChrome/lighthouse-ci)

### 图片优化工具
- [TinyPNG](https://tinypng.com/)
- [Squoosh](https://squoosh.app/)
- [ImageOptim](https://imageoptim.com/mac)

### CDN服务
- [jsDelivr](https://www.jsdelivr.com/)
- [unpkg](https://unpkg.com/)
- [cdnjs](https://cdnjs.com/)
